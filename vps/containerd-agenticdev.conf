[Service]
# containerd normally serves only root/Docker. Keep the socket root-only even
# if a distribution package changes its default umask.
ExecStartPost=/bin/sh -c 'chown root:root /run/containerd/containerd.sock && chmod 0600 /run/containerd/containerd.sock'
